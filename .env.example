# Canvas instance URL. Do not include a trailing slash.
CANVAS_BASE_URL=https://your-school.instructure.com

# Canvas API access token. Keep the real token in .env, not in git.
CANVAS_API_TOKEN=replace-with-your-token
